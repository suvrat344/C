// Create a program to calculate the Area of a Triangle. 
// Area of triangle = ½*B*H

#include <stdio.h>

int main(){
  int base = 12,height = 5;
  printf("Area of Rectangle = %.2f",0.5 * base * height);

  return 0;
}